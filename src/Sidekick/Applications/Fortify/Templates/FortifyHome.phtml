<?php
/**
 * Author: oke.ugwu
 * Date: 27/06/13 18:04
 *
 * @var $this \Sidekick\Applications\Fortify\Views\FortifyHome
 */
?>
<h1>Fortify Home</h1>
<?php foreach($this->builds as $b): ?>
  <div class="well">
    <h2>
      <a
        href="/fortify/<?= $b->project_id ?>/<?= $b->build_id ?>/<?= $b->id; ?>">
        <?= $this->project($b->project_id)->name; ?>: Build #<?= $b->id; ?>
      </a>
    </h2>

    <p><strong>Status: </strong><?= ucfirst($b->result) ?></p>

    <p><strong>Build Type: </strong><?= $b->build_type ?></p>

    <p><strong>Commit Hash: </strong><?= substr($b->commit_hash, 0, 8) ?></p>

    <p><strong>Started: </strong>
      <?= date('d M Y', strtotime($b->start_time)) ?>
    </p>
  </div>
<?php endforeach; ?>
